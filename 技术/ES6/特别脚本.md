```javascript
_res = [];
this.component.list.forEach((i,p) => {
    let letters = [];
    let chars = [];
    letters.title = i.title;
    chars.title = i.title;
    i.choice.forEach(j => {
        if (j.checked) {
            letters.push(j.id);
            chars.push(j.value);
        }
    });
    _res.push({letters, chars})
});

_res.forEach((i,j) => {
    console.log(j+1, i.letters.toString());
});

_res.forEach((i,j) => {
    console.log(i.chars.title);
    console.log(i.chars.join('\n'));
});
```

